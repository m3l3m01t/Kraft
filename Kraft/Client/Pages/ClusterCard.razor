<div>
    <Card>
        <CardImage Source="/assets/images/9.jpg" Alt="Placeholder image" />
        <CardBody>
            <CardTitle Size="5">Cluster @Cluster.Name</CardTitle>
            <CardText>
                @Cluster.Description
            </CardText>
            <Buttons>
                <Button Color="Color.Primary" @onclick="OnConnect">Connect</Button>
                <Button Color="Color.Secondary" Clicked="@OnEditCluster">Modify</Button>
            </Buttons>
        </CardBody>
    </Card>
</div>

@code {
    [Parameter]
    public Kraft.Shared.Cluster Cluster { get; set; }

    protected override Task OnInitializedAsync()
    {
        return Task.CompletedTask;
    }

    [Parameter]
    private EventCallback OnEditCluster { get; set; }

    [Parameter]
    private EventCallback OnConnect { get; set; }
}